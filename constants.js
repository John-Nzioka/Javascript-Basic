//value of variable can change

let interestRate = 0.3;
interestRate = 1;
console.log(interestRate)


//value of the variable cant change because of const

const bargainingRate = 0.3;
//bargainingRate = 1;         //cannot take this line because of const
console.log(interestRate)
